
const initialState = {
  awaitingResponse: null,
  resultMsg: null,
  action: null,
  resultErrorCode: 0
};

const state = { ...initialState };

export default state;

export { initialState, state };

